<Window x:Class="SpellCheckDbTable.Views.DbConnectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cal="http://www.caliburnproject.org"
        Title="DbConnectionManager"
        MinWidth="350"
        MinHeight="100">
    <StackPanel Orientation="Horizontal">
        <StackPanel>
            <Label Content="Server Name:"/>
            <Label Content="Authentication:"/>
            <Label Content="User name:"/>
            <Label Content="Password:"/>
            <Button Content="Connect" Margin="0,17,0,0" cal:Message.Attach="Connect($view)" />
        </StackPanel>
        <StackPanel MinWidth="250">
            <TextBox Name="ServerName" Margin="0,5,0,0"/>
            <ComboBox Margin="0,5,0,0" Name="AuthType">
                <ComboBoxItem Content="Windows Authentication"></ComboBoxItem>
                <ComboBoxItem Content="SQL Server Authentication"></ComboBoxItem>
            </ComboBox>
            <TextBox Name="Username" Margin="0,5,0,0"/>
            <PasswordBox Name="PasswordBox" Margin="0,5,0,0"/>
            <Button Content="Close" Margin="0,15,0,0" cal:Message.Attach="Close" />
        </StackPanel>
    </StackPanel>
</Window>